# --- Temporal ---
TEMPORAL_HOST=localhost:7233
TEMPORAL_NAMESPACE=default

# --- Deployments API (segment allocation, Step 1) ---
# Base host only; the /api/v2/projects/{PROJECT_ID}/groups/{GROUP}/deployments
# path is composed per request from the values below.
DEPLOYMENT_API_URL=https://america.com
DEPLOYMENT_PROJECT_ID=my-project
DEPLOYMENT_GROUP=my-group
