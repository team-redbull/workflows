apiVersion: v2
name: connectivity
description: Connectivity workflow (Temporal workflow + activity workers) — opens firewall rules via the next service and unlocks segments in the Segments Manager
type: application
version: 0.1.0
appVersion: "0.1.0"
