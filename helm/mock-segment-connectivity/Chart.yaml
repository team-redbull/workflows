apiVersion: v2
name: mock-segment-connectivity
description: Mock of the external "next" connectivity (firewall) service (dev/mock-segment-connectivity), deployed for end-to-end testing where the real next service is unreachable. Never used in production — point helm/segment-lifecycle's config.nextUrl at the real service there instead.
type: application
version: 1.0.46
appVersion: "v1.0.46"
